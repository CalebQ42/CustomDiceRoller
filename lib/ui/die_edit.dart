import 'package:flutter/material.dart';

class DieEdit extends StatelessWidget{

  const DieEdit({super.key});

  @override
  Widget build(BuildContext context) =>
    Column(
      children: [
        TextField(),
        // AnimatedBuilder(
        //   animation: animation,
        //   builder: builder
        // )
      ],
    );
}